<%= form_for(@placed_order) do |f| %>
  <% if @placed_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@placed_order.errors.count, "error") %> prohibited this placed_order from being saved:</h2>

      <ul>
      <% @placed_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_placed %><br>
    <%= f.datetime_select :time_placed %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.text_field :customer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
